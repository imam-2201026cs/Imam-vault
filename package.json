{
  "name": "reviseit-deploy",
  "version": "1.0.0",
  "description": "Root package.json to proxy Render deployment to the backend folder",
  "scripts": {
    "build": "cd backend && npm install",
    "start": "cd backend && npm start"
  }
}
